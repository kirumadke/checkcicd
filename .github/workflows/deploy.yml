- name: Deploy via SSH
  uses: appleboy/ssh-action@v0.1.6
  with:
    host: <EC2-IP>
    username: ftpuser
    key: ${{ secrets.FTP_SSH_KEY }}
    port: 22
    script: |
      echo "Connection successful!"

